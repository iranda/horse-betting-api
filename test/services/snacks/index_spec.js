const { expect } = require('chai');
const sinon = require('sinon');
const snackService = require('../../../src/services/snacks');

describe('<fill>', () => {
  it('<fill>', () => {
    expect(false).to.equal(true);
  });
});
